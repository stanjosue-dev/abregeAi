// Ce fichier est injecté dans Gmail en tant que content script.
// Il ne fait rien par lui-même — il permet à executeScript (dans popup.js)
// de cibler la page. La fonction getText() est définie directement dans
// popup.js via func: getText, donc ce fichier peut rester vide pour l'instant.

// peux ajouter des fonctionnalités ici après.
